#include<stdio.h>
struct input
{
	unsigned int cnt;
	unsigned int repeat;
	unsigned char cmd;
	unsigned char len;
	unsigned char data[len];
};

struct output
{
	unsigned int pop_cnt;
	unsigned rep_cnt;
	unsigned char len;
	unsigned char data[len];
};

struct output circular_buffer(struct input *in)
{
	
}	

int main()
{
	struct data *head;
	char choice;
	for(int i=0;i<5;i++)
	{
		head=add_node(head);
	}
}
