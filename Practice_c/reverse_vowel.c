#include<stdio.h>
#include<string.h>

void reversevowel(char *p)
{
	char temp;
	for(i=0,j=strlen(p)-1;p[i];)
	{
		
	}
}
int main()
{
	char str[100];
	printf("enter the string:"):;scanf("%s",str);
	reversevowel(str);
}
